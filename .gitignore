node_modules/
.DS_Store
*.log
examples/bench/state-*.json
examples/bench/*-measurement.json

# 本地待提交的上游 PR 草稿
.contrib/

# seedmux 的会话记录（本地工具产物，不属仓库内容）
.agent-tape/

# Python 字节码（本地跑 py_compile 会生成）
__pycache__/
*.pyc

# 单元测试的输出：每跑一次就新建一个带时间戳的文件，属可再生产物，不入仓库。
# 注：**此前已提交的 test-* 仍在版本控制里**（报告引用它们当证据），gitignore 不影响已跟踪文件。
bench/raw/test-*

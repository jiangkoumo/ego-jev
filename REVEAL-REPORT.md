# 结论：在真实滚动密集站点上，「有界滚动揭示」**一次都没触发**（24 轮 0 次）——`maxReveals=4` 不是瓶颈，真正的卡点是引擎的 `stuck` 守卫（连续 3 次滚动即中止）；A 栈在 X 上 0/14（含首屏内目标），B 栈 6/12，差异可精确定位到该守卫。

**被测 revision：A 栈 commit `4d18c93`（md5 `9b760682cebac9fdde7d4172639a928a`，未改动）；B 栈 `jev-ultrafast` `c32df93`。**

## ① 站点可用性（只读探测，`bench/raw/reveal-probe-*.json`）

| 站点 | 结论 | 依据 |
| --- | --- | --- |
| **X** | **可用（只读）** | ego 侧 `x.com/home` 直接进时间线：`loginWall=false`、`hasTimeline=true`、`docH 10129–12234`（≈15–18 屏）、DOM 内 3–7 条帖子（**虚拟化**）。未登录、未输入、未发布 |
| CNKI | 检索页可加载，未用于任务 | `kns.cnki.net/kns8s/search` 有检索框；需输入关键词且页面含验证码相关文案，风险高；已有 X 满足"真实业务站点"要求 |
| Wikipedia 搜索结果 | 可用（备选站点） | 25–26 条结果，`docH≈3500`（5 屏） |
| GitHub issues / HN p2 | 可用 | 25 行 / `moreLink` 存在 |

X 侧只读纪律：全程未点发布/回复/转发/点赞，未输入任何内容；所有终态 URL 都是 `x.com/home` 或 `x.com/<user>/status/<id>`（无 compose/intent 类 URL）。

## ② 每任务成功率与揭示次数分布（A 栈，5 轮 × 5 任务，24 个有效轮）

| 任务 | 目标屏 | 成功率 | 揭示次数分布 | 失败原因 |
| --- | --- | --- | --- | --- |
| `x-control` | 0.26（**首屏内**） | **0/5** | 0,0,0,0,0 | `stuck`×4、`jev_done`×1 |
| `x-below` | 1.4–1.8 | **0/5** | 0,0,0,0,0 | `stuck`×4、`jev_done`×1 |
| `x-deep` | 2.2–2.8 | **0/4** | 0,0,0,0 | `stuck`×4 |
| `wiki-first` | 0.73（首屏内） | **5/5** | 0,0,0,0,0 | —（中位 1027ms、1 步、1 次 Jev） |
| `wiki-deep` | 2.02 | **0/5** | 0,0,0,0,0 | `blocked`×5（第 1 步就放弃） |

- **揭示次数总计 0 次**（`{0: 24}`）。失败原因分布：`stuck` 12、`blocked` 5、`jev_done` 2（误判完成）、成功 5。
- 动作序列分布直接指向卡点：`scroll_down×4` = **10 轮**，其余为滚动变体（含 `scroll_up`）3 轮、`blocked` 4 轮、`click:ref=23` 5 轮（wiki-first 的成功）。

## ③ 揭示的额外代价

**测不出来，因为揭示从未触发。** 唯一可比的是 Wikipedia 的对照：首屏内 1027ms / 1 步 / 1 次 Jev；首屏外目标在 1 步内即被 Jev 判 `blocked`（未滚动），因此"揭示代价"为 0 —— 不是因为它便宜，而是因为它没工作。

## ④ 默认 `maxReveals=4` 够不够 / 建议值

- **没有任何一轮触到 `maxReveals` 上限**（0 次揭示），所以"触顶失败样本"为 0；按工单要求单独记录：**不存在触顶样本**，目标屏数依次为 0.26 / 1.4–1.8 / 2.2–2.8 / 2.02。
- **建议：不要为提高该上限而调参**（无证据支持，且调大只会增加潜在载荷与步数）。真正的两个限制是：
  1. **`stuck` 守卫**：A 的"页面是否变化"只看 URL（`stableUrl(urlAfter)!==stableUrl(urlBefore)`），滚动不改 URL → 连续 3 次同动作即判 `stuck`。实测 X 上第 4 次滚动就被中止。对照 B 栈：其 `fingerprint()` **把 `scroll` 位置算进指纹**，滚动即视为"页面已变化"，所以 B 不会因滚动中止 —— 这正是 A 0/14、B 6/12 的机制差异。
  2. **揭示的触发条件太窄**：它只在"选了需目标的动作但解析不出目标（`targetMissing`）"或"元素表为空"时生效；真实长列表里表内总有大量相似链接，Jev 会**点错**或**直接滚**，不会选 `none`，于是揭示永不触发。

## ⑤ B 栈对照（12 轮，`bench/raw/reveal-bstack-*.jsonl`）

| 任务 | B 成功率 | 说明 |
| --- | --- | --- |
| `x-first`（首屏内帖子） | **6/12** | 两个批次差异极大：批次 1 **0/3**（`blocked`，0 动作），批次 2 **3/3**（中位 2908ms、3–4 步） |
| `x-scroll`（首屏外） | **6/12** | 同上：批次 1 **0/3**（2 轮还撞上 X 自身的 `x.com/home?failedScript=vendor` 错误页），批次 2 **3/3**（中位 3586ms、5–9 步，确实滚动后点开） |
| `wiki-first` | 3/3 | 中位 1226ms、1 步（与 A 的 1027ms 同级） |
| `wiki-deep` | 0/3 | 全部 `blocked`（与 A 相同） |

**结论**：X 上 B 能完成（含需要滚动的目标），A 完全不能；但 B 也不稳定（6 轮全失败 ↔ 6 轮全成功，与 X 页面/脚本状态有关）。Wiki 上两栈表现一致（首屏内可、首屏外都放弃）。

## ⑥ 限制

1. 每个任务只有 5 轮（B 3 轮）；X 时间线每次刷新内容都在变，锚点按轮次现场测量，不同轮的目标不同帖。
2. X 的 `failedScript=vendor` 错误页与 DOM 虚拟化（同时只渲染 3–7 条）让"目标在第几屏"只是**测量时刻**的近似值。
3. **未改引擎**（工单要求）。发现的 2 个缺陷只记录不修：① 元素名/值按 UTF-16 `slice(0,60)` 截断，emoji 被切出孤立代理 → Jev 请求 400 `invalid Unicode text` → 整轮 `action_failed`（实测 X 时间线 **235 项里 5 项**命中）；② 上述 `stuck` 守卫。
4. 未验证 CNKI 的真实检索流程（需输入关键词 + 验证码风险），因此"第二个真实业务站点"用 Wikipedia 搜索结果替代。

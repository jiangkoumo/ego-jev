# 结论：视口外目标已可到达——`hn-page2` 从 **0/10 变为 10/10**（同会话配对，中位 2854ms / 4 步），且三项既有收益与延迟导航兜底**均无回归**（配对 bootstrap CI 全部跨 0），元素表大小与 `maxTargets` 默认值未变。

**新引擎 revision：commit `4d18c93`**（父提交 `d08118e`）。改动前 md5 `ab92e702acfc42d0b51853691c218dd6`（= commit `70b566b`），改动后 md5 `9b760682cebac9fdde7d4172639a928a`。
`PORT-REPORT.md` 与 `VERIFY-REPORT.md` 里的数字**全部对应改动前 revision `70b566b`**，本报告数字对应 `4d18c93`；旧报告未改动。

## 做了什么 / 为什么

根因（复现见 `bench/raw/fix-analysis-*.txt` 与旧 `VERIFY-REPORT.md`）：元素表只收**当前视口内**的候选，并在**文档顺序**上截断到 `maxTargets`（默认 60）；HN 的 `More` 链接在文档序第 **110/119** 位，因此**从不进入候选表**，Jev 只能反复选到 `none` → `target_missing`。**关键**：单纯"滚动再观测"治不好，因为滚动后仍在文档序前 60 里打转。

两处改动（均在 `scripts/ego-jev.mjs`）：

1. **观测层：候选超出预算时，优先列出本次尚未展示过的元素**（各组内保持 DOM 顺序），已展示过的只用于补位。于是"滚动一屏 → 重新观测"总能露出**下一批**。内部收集上限为 `limit×4`（≤240），有界；`shown` 集合超 1200 时按"仍在文档内"剪枝。候选不超预算的页面（wiki 43–45 项、下拉页 2 项、表单页 10 余项）顺序与旧版**完全一致**。
2. **循环层：有界滚动揭示**。某步"选了需目标的动作却没解析出目标"（或元素表为空）时，滚动约一屏（`0.85×视口高`）后重新观测再决策；**默认最多 4 次**（`maxReveals` 可调），到页面边界即止，**滚动没带来任何新元素时立即停止并把该次计入 `no_progress`**（不允许用滚动无限续命）。

## 取舍（明确的）

- **没有调大 `maxTargets` 默认值**（仍是 60）：元素表大小、进 Jev 的载荷与 token 成本都不变。代价是"一次观测看不全"，用**多次有界观测**换取覆盖，而不是用更大的表。
- **代价**：触发揭示的那一步会多一次 Jev 请求与一次滚动（实测 `hn-page2` 用 4 步 / 4 次 Jev，其中 1 步是 Jev 自己选中的无效 `click`）；不触发揭示的任务（三项回归任务）零额外开销。
- **没有改**观测/动作层结构、没有回退 `page.snapshot()`；退出原因语义保持：够不到仍报 `target_missing`（未新增原因，无需新增）。
- 未选"惰性包含视口外元素"方案：那会直接增大表并需要额外的滚动到元素逻辑，与本任务"不牺牲小表收益"的约束冲突。

## 新增能力数据（`bench/fix-hn-page2.js` → `raw/fix-hn-page2-2026-09-19T16-09-20-533Z.json`）

10 轮 × {改动前, 改动后} 交替，判据沿用 `bench/verify-protocol.md`（终态 URL 含 `p=2`）：

| 引擎 | 成功率 | 成功中位 | 步中位 | Jev 中位 | 失败原因 |
| --- | --- | --- | --- | --- | --- |
| 改动前 `70b566b` | **0/10** | — | — | — | `target_missing` ×10 |
| 改动后 `4d18c93` | **10/10** | 2854ms | 4 | 4 | — |

动作序列稳定为：`scroll_down`（Jev）→ `click`(none) → *引擎揭示滚动* → `click:ref=221`（More）。

## 无回归数据（`bench/fix-noregress.js` → `raw/fix-noregress-2026-09-19T16-07-36-956Z.json`）

同会话配对（同轮交替、奇偶轮翻转先后），每任务 6 轮 × 2 引擎，配对差值 = 新 − 旧：

| 任务 | 旧成功率/中位 | 新成功率/中位 | 配对中位差 | bootstrap 95% CI | 跨 0? | 新胜/旧胜 |
| --- | --- | --- | --- | --- | --- | --- |
| `hn-nav` | 6/6 · 1528ms | 6/6 · 1819ms | +114ms | [−513, 473] | **是** | 3/3 |
| `wiki-search` | 6/6 · 3684ms | 6/6 · 3606ms | −160ms | [−1171, 82] | **是** | 5/1 |
| `select-native` | 6/6 · 1095ms | 6/6 · 1189ms | +38ms | [−561, 245] | **是** | 3/3 |

三个任务的 CI 全部跨 0 → **未检出回归**；差值明细里正负都有（如 hn-nav [−756, −19, +247, +335, −271, +610]），量级与 Jev 单次 400–2000ms 的抖动一致。

- **延迟导航兜底**（`bench/delayed-nav.js` → `raw/delayed-nav-2026-09-19T16-10-06-198Z.json`）：新引擎两种等待模式均 **3/3 成功**（旧 pre-port 引擎 0/3，原因随 Jev 选择在 `stuck`/`action_failed` 间变化）。
- **护栏自测**：`node bench/test-guardrails.mjs` **23/23 通过**；13 个导出全部保留。

## 限制

1. 揭示额度默认 4 次（最多扫 4×60=240 个候选）。超长列表（如 X 时间线）上目标若在 240 个候选之外仍需更多次滚动，可调 `maxReveals`；未在真实 X/CNKI 页面上验证。
2. 揭示会消耗一个步进名额并多一次 Jev 请求（`hn-page2` 4 步 vs 理想的 2–3 步），换取的是"能到达"。
3. `hn-page2` 上 B 栈仍 10/10 `blocked`（首轮 0 动作），本任务未以 B 为参照。
4. 未改动的旧文档问题（供参考）：`SKILL.md` 里"每步浏览器调用：1 次 `page.snapshot()` + 1 次批量 `page.evaluate()`"一句描述的是**移植前**行为，与现状不符；因不属本次范围，未修改。

# 结论：大样本配对验证（n=10/格，冻结引擎 70b566b）给出三个真实差异与一个真"无差异"——A 栈在点击链（−378ms）与搜索（−819ms，且只花 1 次 Jev 决策 vs B 的 3 次）上更快，B 栈在原生下拉上更快（+248ms），表单任务**无差异**；翻页任务两栈都完不成，是能力边界而非速度问题。

## 方法（预登记，见 `bench/verify-protocol.md`，写于采集前）

| 项 | 值 |
| --- | --- |
| A 栈 | ego lite + `ego-jev` **commit `70b566b`**，采集前后 md5 均为 `ab92e702acfc42d0b51853691c218dd6`（未改动） |
| B 栈 | browser-harness + `jev-ultrafast` **commit `c32df93`**（v0.1.0） |
| 模型 | Jev `jev-latest`（api.typesafe.ai）；文本模型两栈同一个 opencode zen / `minimax-m3` |
| 样本 | 5 任务 × 2 栈 × 10 轮 = **100 轮**，A/B 交替且奇偶轮翻转先后顺序 |
| 判据 | 单一版本，只用终态 URL / 页面断言，不依赖"点了哪个按钮"（上一轮的教训） |
| 统计 | 中位 + IQR + 极值；配对 `d=A−B` → 中位差 + bootstrap 95% CI（10000 次，种子 20260919）+ 符号检验 |
| 判定 | **CI 跨 0 → 写"无差异"**（预先指定，未事后更改） |
| 数据 | `bench/raw/verify-2026-09-19T15-39-12.jsonl`（100 行）；统计 `raw/verify-analysis-2026-09-19T15-39-12.{json,txt}` |

## 每任务结论

| 任务 | 交互类型 | A 成功率 / 中位(IQR) | B 成功率 / 中位(IQR) | 配对轮 | A−B 中位差 | bootstrap 95% CI | 跨 0? | A胜/B胜 | 符号检验 p | **结论** |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `hn-nav` | 点击链（2 链接） | 10/10 · 2106ms (2032–2511) | 10/10 · 2445ms (2414–3338) | 10 | **−378ms** | [−1453, −313] | 否 | 10/0 | 0.002 | **A 更快（稳健）** |
| `wiki-search` | 文本生成+输入+提交 | 10/10 · 3956ms (3566–4136) | 10/10 · 4771ms (4467–5156) | 10 | **−819ms** | [−1314, −195] | 否 | 8/2 | 0.109 | **A 更快**（见注） |
| `httpbin-form` | 填写+勾选+提交 | 9/10 · 4565ms (4198–5002) | 8/10 · 3558ms (3461–4291) | 7 | +652ms | [−587, 859] | **是** | 2/5 | 0.453 | **无差异** |
| `select-native` | 原生 `<select>` 改选 | 10/10 · 1509ms (1489–1577) | 10/10 · 1260ms (1240–1357) | 10 | **+248ms** | [163, 426] | 否 | 1/9 | 0.021 | **B 更快（稳健）** |
| `hn-page2` | 翻页（目标在视口外） | **0/10** | **0/10** | 0 | — | — | — | — | — | **两栈均未完成**（能力边界） |

注：`wiki-search` 的 CI 不跨 0（按预登记规则判为"A 更快"），但符号检验 8/2 的 p=0.109 未达 0.05——即"幅度稳健、方向一致性不显著"，两个统计量在此不完全一致，如实并列。
`hn-nav` 的 B 侧有两个离群轮（7231ms、5149ms，来自单次 Jev 决策 1–2s 的抖动），中位差因此比均值差（−934ms）保守。

## 噪声源（为什么次数比单次延迟更关键）

| 任务 | A Jev 次数中位 | B Jev 次数中位 | B 单次 Jev 延迟中位 |
| --- | --- | --- | --- |
| hn-nav | 2 | 2 | 1051ms |
| wiki-search | **1** | **3** | 527ms |
| httpbin-form | 3 | 3 | 426ms |
| select-native | 2 | 2 | 761ms |

- 搜索任务是 A 领先的主因：A 的 `type_text_submit` 在**一次请求**里同时定操作与目标，B 需要 3 次决策（文本模型调用两者相当）。
- 下拉任务 B 领先的机制：A 在动作后额外花 ~160ms 的 settle（40ms 静默 + 轮询 URL/readyState）并多一次 `locateForInput` 校验，B 只等 50ms 就重新观测；逐步看 A 步1 中位 1030ms vs B 797ms、步2 446ms vs 361ms，两步都慢。

## 能力边界：`hn-page2`（两栈都失败，原因不同，均已定位）

- **A 侧 10/10 `target_missing`**：HN 的 `More` 链接在文档顺序里是**第 110 个可交互元素**（同页共 119 个），而引擎元素表默认上限 **60**，因此该链接从不进入候选表——Jev 只能反复选到 `none`。证据：`bench/raw/verify-*.jsonl` 中 A 的 `hn-page2` 全部 `target_missing`；机制核实见下方"复现"。
- **B 侧 10/10 `status=blocked` 且 `steps=0`**：B 的观测同样只覆盖视口，首轮 Jev 直接判 `blocked`（一次动作都没执行）。
- 这不是速度差异，**不出配对结论**；提升 A 的 `maxTargets` 是调用方选项（一行参数，非引擎改动），本次按预登记的默认配置测量。

## 复现（机制核实，非样本）

```
# More 在文档顺序中的位置 vs 引擎上限（bench 脚本一次 evaluate）
不限量共 119 条，More 在第 110 位（url=https://news.ycombinator.com/?p=2）
limit=60 时元素表条数 60，含 More = false
```

## 失败与重试（不得静默丢弃）

- **重试轮次 0**：本次 100 轮全部一次成功产出（上一轮的 browser-harness 5s IPC 超时未再出现；编排在每轮间加了 1s 间隔）。
- 任务级失败 23 轮全部保留：A 11 轮（`hn-page2`×10 `target_missing`、`httpbin-form`#5 `no_targets`）；B 12 轮（`hn-page2`×10 `blocked`、`httpbin-form` #1/#2 `blocked` 且 0 动作）。
- `httpbin-form` 两栈各有随机失败（A 9/10、B 8/10），n=10 下 9 vs 8 不构成差异证据，已计入成功率但不作结论。

## 限制

1. `hn-page2` 两栈皆 0/10，样本只证明"两栈都完不成"，未测出谁更快。
2. A 侧未记录单次 Jev 延迟（预登记时已知，仅 B 可拆解），故 A 的"决策耗时 vs 总耗时"无法分离。
3. 单机单网络、Chrome 单实例；Jev 单次 400–2000ms 的抖动仍在（`hn-nav` B 侧 7.2s 离群轮即为此）。
4. 配对仅在"两栈同轮都成功"时成立（`httpbin-form` 因此只剩 7 轮），成功率差异本身未做检验。
5. `wiki-search` 判据覆盖两个合法终态（文章页与搜索结果页），本次 20 轮全部落在文章页。

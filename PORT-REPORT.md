# 结论：把 jev-ultrafast 的决策闭环移植进 ego-jev 后，A 栈把移植前约 2 倍的差距追平，并在导航类与搜索类任务上稳定领先 B 栈（HN 两步导航 3 批全胜、维基搜索 3 批全胜且少 2 次 Jev 请求）；表单类任务打平略劣。建议默认用 A 栈。

## 0. 口径与复现

- A 栈 = `ego lite + scripts/ego-jev.mjs`（本次改的对象）；B 栈 = `browser-harness + jev-ultrafast`。
- 同一个 Jev（api.typesafe.ai/v1，jev-latest=jev-1.13.0）、同一个文本模型（opencode zen / minimax-m3）、同一台机器、同一个 Chrome。
- 计时口径：页面加载完成后再计时，逐步决策；每步之后与每轮开始前各查一次成功条件。
- 原始数据全部在 `bench/raw/`；脚本在 `bench/`；改前的引擎备份为 `bench/baseline-engine.mjs`。
- §3 的对照统计由 `bench/analyze-vs-bstack.mjs` 从 3 个批次文件合并得出（`raw/vs-bstack-merged.json` / `.txt`），不是手抄。

## 1. 先补完的 A/B：等待策略单独收益（HN 两步导航，改前引擎）

`bench/settle-ab.js` → `raw/settle-ab-2026-09-19T13-52-20-701Z.json`

| 等待策略 | 端到端中位 | 每步中位 | 成功 |
| --- | --- | --- | --- |
| 300ms + navWait 1200ms（原默认） | 3923ms | 2460ms | 3/3 |
| 50ms + navWait 250ms | 3394ms | 1616ms | 3/3 |

**等待策略每步只值 ~265ms**，而当时 HN 端到端与 B 栈差 ~1100ms/步 → 大头不在这里。
（上一次跑到一半中断的原因已定位：`taskSpace` 按名字 resume，上一轮已 finish 的空间里没有 `p1`。）

分解探针（`bench/probe-*.js` → `raw/probe-*.json`）找到了大头：

| 观测 | 实测 | 依据文件 |
| --- | --- | --- |
| `page.snapshot()`（HN，27484 字符） | 110–130ms | probe-latency |
| 一次 `page.evaluate` 自建元素表（60 项 / ~1.8k 字符） | 2ms | probe-latency |
| **`page.click(ref)` 点一个链接** | **788–1005ms** | probe-click2 |
| `page.mouse.click(x,y)` | 872–912ms | probe-click2 |
| **裸 `Input.dispatchMouseEvent` 两次** | **13–16ms** | probe-click2 |
| Jev 单次决策 | 497–2036ms，与载荷大小无关（小载荷反而更慢的样本存在） | probe-latency |
| `page.fill` vs 裸 CDP（click+selectAll+insertText） | 163ms vs ~50ms | probe-raw |

→ 真正的瓶颈是 **ego 的语义动作通道**（每次点击内部要重跑快照解析/等导航/驱动可见光标）。
元素表本身只值 ~110ms/步；它的价值在于**把 DOM 节点身份拿回代码手里**，从而解锁坐标级裸 CDP 派发。

## 2. 每项改动的单独收益（3 任务 × 3 次，全部 3/3 成功）

`bench/isolation.js` → `raw/isolation-2026-09-19T15-05-15-194Z.json`（A0/A1 为对照臂）

| 臂（逐项打开） | HN 中位 | HN 每步中位 | httpbin 中位 | wiki 中位 |
| --- | --- | --- | --- | --- |
| A0 改前引擎（快照+300/1200+无校验） | 4569ms | 2322ms | 6782ms | 3663ms |
| A1 新引擎但全部回退旧行为（对照） | 4534ms | 2231ms | 8343ms | 3915ms |
| A2 + 可观察等待 | 3382ms | 1959ms | 5706ms | 3923ms |
| A3 + 自建 DOM 元素表 + 裸 CDP 动作 | 1675ms | 827ms | 3337ms | 3760ms |
| A4 + 响应校验 + 提示词规则 + 执行前守卫 | 1916ms | 921ms | 3607ms | 3634ms |

- **Δ等待策略**：HN −1152ms（−25%）；httpbin −2637ms。单项收益，且不牺牲正确性。
- **Δ观测层/动作路径**：HN −1707ms（−50%）；httpbin −2369ms。**这是最大的一项**。
- **Δ校验与提示词**：HN +241ms（A4 与 A3 区间重叠 [1767..1985] vs [1616..1787]，视为无影响）。
  它的价值是**安全网**而非速度：45 次真实运行 90 步里 0 次误拒，异常路径由 `bench/test-guardrails.mjs` 证明（23/23 通过，`raw/test-guardrails.txt`）。
- 代价：浏览器调用次数上升（HN 12→18，httpbin 24→35），因为等待改成了轮询；单次调用 ~1ms，净收益仍为大头。
- wiki 三个臂都在 3.6–3.9s，差异落在文本模型 + Jev 的延迟方差里，**不构成有效对比**。

## 3. A 栈（移植后）vs B 栈对照（全量 3 批合并，每格 n=9）

`bench/vs-bstack.sh` 跑了 3 个完整批次；合并统计见 `bench/analyze-vs-bstack.mjs` → `raw/vs-bstack-merged.json`（人读版 `raw/vs-bstack-merged.txt`）。

| 任务 | A 栈中位 | B 栈中位 | 差异 | A 步/Jev | B 步/Jev | 逐批胜负 |
| --- | --- | --- | --- | --- | --- | --- |
| HN 两步导航 | **2446ms** | 2546ms | A 快 3.9% | 2 / 2 | 2 / 2 | 3 批全 A 胜 |
| 维基搜索（含文本生成） | **4114ms** | 4710ms | A 快 12.7% | **1 / 1** | 3 / 3 | 3 批全 A 胜 |
| httpbin 表单（填写+勾选+提交） | 4606ms | **4370ms** | A 慢 5.4% | 3 / 3 | 3 / 3 | 2 批 B 胜、1 批 A 胜 |

**批次披露（3 批不是同一条件，必须分开看）**：

| 批次文件（`raw/`） | 引擎版本 | wiki 判据 | 说明 |
| --- | --- | --- | --- |
| `vs-bstack-2026-09-19T14-51-12.jsonl` | rev1 | **旧窄判据** | A 的 wiki 被记为 0/3，见下 |
| `vs-bstack-2026-09-19T14-54-32.jsonl` | rev1 | 新宽判据 | 仅改判据，未改引擎 |
| `vs-bstack-2026-09-19T14-57-26.jsonl` | rev2 | 新宽判据 | rev2 = settle 的导航后等待从 `load` 改为 `domcontentloaded` |

- 还有一个 `vs-bstack-2026-09-19T14-48-24.jsonl` 被丢弃：0 行有效数据（编排脚本没接住 ego 走 stderr 的 `console.log`，且 browser-harness 撞 5s IPC 超时）；修好后才有上面 3 批。
- **wiki 判据变更必须披露**：旧判据只认 `/wiki/JEV`、`index.php?search=`、`Special:Search`。A 在搜索框按**回车**提交，落到重定向目标 `/wiki/Japanese_encephalitis`（B 点 Search 按钮，落到 `/wiki/JEV`）——两者都是“搜索已提交”的合法终态，旧判据实际测的是“按了哪个按钮”。
- 因此按新判据从各批记录的 `finalUrl` **重算**（不是重跑）：A 的 wiki 在 3 批里分别是 3/3、3/3、3/3；若沿用旧窄判据则是 0/3、0/3、2/3（合并 2/9，B 为 9/9）。
- 引擎版本影响：批次 1–2 用 rev1，批次 3 用 rev2。httpbin 只在批次 3（rev2）A 胜，而 rev2 恰好缩短了“导航后等待”（httpbin 最后一步是 POST 后跳转）——所以这一胜很可能归因于 rev2，**不能当作“A 在表单任务上更优”的证据**。
- 方差：n=3/格；Jev 单次 400–2000ms、文本模型 1.1–3.3s 的抖动是主噪声源（B 栈有一轮 10.1s 的 wiki 样本）。hn-nav 的 3.9% 领先幅度接近噪声，但 3 批方向一致，仍算稳健。
- A 栈在 wiki 上只用 1 步：`type_text_submit` 在**一次请求**里同时定操作与目标，B 栈需要 3 步（这是 A 栈请求结构的固有优势，也是它领先的主要来源）。

## 4. 延迟导航兜底（`settle()` 当初存在的理由）没有被短等待丢掉

`bench/delayed-nav.js`（本地 `http://127.0.0.1:8099/a.html`：改选下拉 → 250ms 后才出现确认按钮 → 点击后 350ms 才真正跳转）→ `raw/delayed-nav-2026-09-19T15-00-41-870Z.json`

| 臂 | 成功 | 步数 | 中位 |
| --- | --- | --- | --- |
| 改前引擎（300/1200） | **0/3**（a11y 快照丢掉了 `<select>` 的选项，只会反复 scroll，stuck） | 4 | 2889ms |
| 新引擎 + 旧等待 | 3/3 | 3 | 1870ms |
| 新引擎 + 可观察等待 | 3/3 | 4 | 2429ms |

结论：短等待**不会**把延迟导航误判成“页面无变化”。代价是比旧等待多 1 步；兜底由三条机制共同保证——
① 下一轮观测遇到“文档正在导航”会等 load 并重试；② 每步动作后立刻复查一次成功条件；③ 守卫拒绝不再计入“无进展”。

## 5. 建议

- **默认栈：A（ego lite + ego-jev）**。理由按证据强度排序：
  1. 导航类任务（HN）3 批全胜，幅度小（3.9%）但方向一致；
  2. 搜索类任务（维基）3 批全胜（12.7%），且只用 1 步 / 1 次 Jev 请求，B 需 3 步 / 3 次——少两次模型请求比省几百毫秒更有价值（成本、延迟尾部、失败面都更小）；
  3. 移植前的 A 栈在同一任务上要 4.5s、B 只要 2.5s（见 §2 A0/A1），移植本身消掉了这个差距，这是本次的主要收益。
- **何时切 B**：① 表单/多控件类流程（httpbin 上 B 略快 5.4%，2/3 批胜）；② 目标浏览器不是 ego lite，只有任意 CDP 端点可用（B 走 browser-harness，与浏览器解耦）；③ 需要 B 自带的截图/录制与逐节点 guard 相等性校验；④ 需要跟随上游 jev-ultrafast 的新改动。
- **何时两者都不用**：需要视觉/坐标以外的复杂交互（文件上传、跨域 iframe 深操作）时，ego 的语义选择器通道更稳。
- **不主张的结论**：“A 在三个任务上都快 18%”不被全量数据支撑——那个数字只来自批次 3（rev2 + 新判据），且 httpbin 合并后是 A 慢 5.4%。

## 6. 尚未解决 / 已知限制

1. 响应校验在 90 步真实运行里**一次都没触发**（0 误拒也 0 命中）——它的收益目前只有合成测试证据，没有线上证据。
2. 延迟导航场景比旧等待多 1 步（4 vs 3），静默窗口 120ms 是速度与步数的折中，未做自适应。
3. 页面可见文本上限 2500 字符（B 栈 6000）；未验证这是否在文本密集任务上损失决策质量。
4. 未移植：截图/录制、B 的 250 项动作上限、`recent_actions` 的原始形状（A 栈沿用自有的 progress 回填）。
5. 两个 ego 运行时的坑已写进代码注释：`page.evaluate` 返回 Promise 会被当成不可序列化值干等 2s；
   后台标签页 rAF 被节流（每次 ~0.9–1.2s），所以“等一帧”放在 Node 侧。
6. 样本量：isolation 为 n=3/格，§3 对照为 n=9/格（3 批 × 3 轮）；单机单网络，Jev 与文本模型的延迟方差大于多数改动之间的差异，绝对毫秒不可外推。
7. 任务成功判据是人工定义的：wiki 的判据宽窄直接改变成功数（旧窄判据 A 2/9、新宽判据 A 9/9），而两栈走的是不同按钮、落到不同合法终态——这部分是**口径选择**，不是引擎差异，已在 §3 披露。
8. §3 的 3 个批次里有 2 个引擎版本与 2 种 wiki 判据，严格讲不是同一个受控实验；合并中位数只能当作方向性证据。
